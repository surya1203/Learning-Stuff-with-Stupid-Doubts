Wrap each of the 4 blocks of code in function definitions, then use the timeit module to time each one.

Remember to import timeit at the start of the file.

My solution will use a slightly different approach, to save time in the video.  The test of whether your solution works is if it successfully times all 4 approaches to capitalising the characters and words in text.